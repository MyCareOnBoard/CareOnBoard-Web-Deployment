import LogoHeader from './LogoHeader'; import { useState } from 'react'; export default function SlideFour(){ const mini = new Array(5).fill(0).map((_,i)=>({title:`Mini ${i+1}`, img:'https://via.placeholder.com/220'})); const [i,setI]=useState(0); return (<div className="text-center px-6"><LogoHeader /><img src={mini[i].img} className="w-48 h-36 object-cover rounded-md mb-3"/><h4 className="text-lg font-semibold">{mini[i].title}</h4><div className="flex items-center gap-4 mt-4"><button onClick={()=>setI(p=>Math.max(0,p-1))} disabled={i===0} className="text-sm text-gray-600 disabled:opacity-40">Prev</button><div className="flex gap-2">{mini.map((_,idx)=>(<span key={idx} className={`h-2 rounded-full ${idx===i?'w-6 bg-[#00B6B3]':'w-2 bg-gray-300'}`}/>))}</div><button onClick={()=>setI(p=>Math.min(p+1,mini.length-1))} className="flex items-center gap-2 bg-[#00B6B3] text-white px-3 py-1 rounded-full">Next</button></div></div>); }