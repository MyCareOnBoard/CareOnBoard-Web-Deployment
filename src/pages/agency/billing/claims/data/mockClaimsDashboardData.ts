export type RecentClaim = {
  id: string;
  client: string;
  clientId?: string;
  clientAvatarUrl?: string;
  staffId: string;
  serviceCode: string;
  paNumber: string;
  serviceDate: string;
  durationStart: string;
  durationEnd: string;
  totalHours: string;
  rate: string;
};
